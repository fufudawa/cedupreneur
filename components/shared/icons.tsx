import { UserRound, GraduationCap, Store } from "lucide-react";

export function DosenIcon() {
  return <UserRound size={32} strokeWidth={2} />;
}

export function MahasiswaIcon() {
  return <GraduationCap size={32} strokeWidth={2} />;
}

export function UmkmIcon() {
  return <Store size={32} strokeWidth={2} />;
}
